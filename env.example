# Databricks CLI profile to use for authentication (see ~/.databrickscfg)
DATABRICKS_CONFIG_PROFILE=DEFAULT

# (Optional) Override the backend WebSocket URL during local development.
# When unset, the frontend auto-discovers the backend port.
# VITE_BACKEND_WS_URL=ws://localhost:8000
